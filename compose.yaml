# Use this file for local test.

services:
  udptk:
    init: true
    build:
      context: .
      target: final
      dockerfile: Dockerfile.dev
    environment:
      RUST_LOG: udptk=debug
    ports:
      - 6777:6777/udp
    command: [ "listen", "-p", "6777"]
  send:
    build:
      context: .
      target: final
      dockerfile: Dockerfile.dev
    environment:
      RUST_LOG: udptk=debug
    ports:
      - 6777:6777/udp
    command: send -d udptk -p 6777 yahaha
